# Test Environment Variables
# Copy this file to .env.test to enable integration tests that require real API keys

# Anthropic API Key (for Claude models)
# Get your key at: https://console.anthropic.com/settings/keys
# Integration tests will be skipped if this is not set or is a dummy value
ANTHROPIC_API_KEY=your_anthropic_key_here

# Google Gemini API Key (optional)
# Get your key at: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_key_here

# OpenAI API Key (optional)
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_key_here

# NOTE:
# - If .env.test doesn't exist, tests will load from .env as fallback
# - If no real keys are provided, API-dependent integration tests will be skipped
# - Unit tests and non-API integration tests will still run with dummy keys
